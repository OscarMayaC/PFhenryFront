/// <reference types="react" />
import { TimeClockProps } from '../TimeClock';
import { DateOrTimeView } from '../internals';
export interface TimeViewRendererProps<TDate, TView extends DateOrTimeView> extends Omit<TimeClockProps<TDate>, 'views' | 'openTo' | 'view' | 'onViewChange'> {
    view: TView;
    onViewChange?: (view: TView) => void;
    views: readonly TView[];
}
export declare const renderTimeViewClock: <TDate extends unknown>({ view, onViewChange, views, value, defaultValue, onChange, className, classes, disableFuture, disablePast, minTime, maxTime, shouldDisableTime, shouldDisableClock, minutesStep, ampm, ampmInClock, components, componentsProps, slots, slotProps, readOnly, disabled, sx, autoFocus, showViewSwitcher, disableIgnoringDatePartForTimeValidation, }: TimeViewRendererProps<TDate, any>) => JSX.Element;
